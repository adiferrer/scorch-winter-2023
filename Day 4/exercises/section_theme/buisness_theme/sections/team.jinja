
{% for member in team %} <!--Lets you iterate through each page -->
            <!-- {{ member[0] }} --> <!--Metadata access -->
            <!-- {{ member[1] | safe }} --> <!--content access -->
<!-- Start of card -->
<div class="col-lg-4 text-center">
    <img src="{{member[0]['image']}}" alt="" width="172px" height="172px" style="border-radius: 50%;object-fit:cover; object-position: top;">
    <h2 class="fw-normal">{{member[0]['name']}}</h2>
    <p>{{ member[1] | safe }}</p>
</div>
<!-- end of card -->
{% endfor %}